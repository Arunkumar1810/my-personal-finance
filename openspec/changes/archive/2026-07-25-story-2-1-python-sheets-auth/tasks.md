## 1. Project Setup

- [x] 1.1 Create basic python project structure (e.g. backend directory)
- [x] 1.2 Create `requirements.txt` with `gspread` and `google-auth`

## 2. Authentication Scaffolding

- [x] 2.1 Write `main.py` (or similar entrypoint) and set up Google Sheets authentication using a service account credentials JSON.
- [x] 2.2 Validate connection to the API (e.g., attempt to open the target Spreadsheet).

## 3. Read/Write Operations

- [x] 3.1 Implement reading the list of tickers from the designated 'Pending Watchlist' sheet.
- [x] 3.2 Implement writing a test/debug string to a specific cell or column to verify write access.
- [x] 3.3 Ensure the script logs its successful operations to the console.
