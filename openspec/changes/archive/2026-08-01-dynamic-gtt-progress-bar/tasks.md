## 1. GttProgressBar Component Updates

- [x] 1.1 Update `GttProgressBar.tsx` to verify `currentPrice` is available (or add it to props if missing).
- [x] 1.2 Calculate the percentage positions for the Buy price and Current Market Price relative to the SL (0%) and Target (100%) bounds.
- [x] 1.3 Render absolute positioned text labels for SL, Target, Buy Price, and CMP inside or adjacent to the progress bar container.
- [x] 1.4 Format the numerical values to 2 decimal places.
- [x] 1.5 Apply appropriate CSS classes (`text-[10px]` or `text-xs`) and neutral/theme colors so the labels are permanently visible and legible.
- [x] 1.6 Test UI responsiveness to ensure labels do not completely obscure each other under normal conditions.
