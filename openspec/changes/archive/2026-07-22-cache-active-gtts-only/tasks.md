## 1. Core Implementation

- [x] 1.1 Update `fetch_and_cache_gtt_orders(kite)` in `backend/kite_client.py` to filter `gtt_orders` where `status == 'active'` before caching and returning.

## 2. Verification

- [x] 2.1 Ensure the frontend GTT dashboard correctly displays only active GTTs.
